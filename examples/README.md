# Examples

- `hello-react-counter`: React + TypeScript “Hello World” counter that can talk to the Qt backend via `QWebChannel` (and falls back to a mock backend in `npm run frontend:dev`). Uses `src/` for the frontend and `src-taqyon/` for the Qt backend.
